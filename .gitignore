# Build output (xcodebuild -derivedDataPath build)
build/

# Xcode
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
*.xcuserstate

# macOS
.DS_Store
*.swp
*~

# Swift Package Manager
.build/
Packages/

# Misc
*.ipa
*.dSYM.zip
*.dSYM
test.txt

# Settings.json backups created by install-hooks.sh
*.bak.*

# Clawd mascot art (clawd-on-desk, AGPL-3.0) is now TRACKED — this project moved
# from MIT to AGPL-3.0 in v1.3.0 specifically so the art can ship with it and every
# user gets the mascot on clone. See NOTICE. Do NOT re-add an ignore rule here
# without relicensing back, or the app renders an empty notch again.
